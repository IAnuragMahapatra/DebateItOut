[project]
name = "debate-it-out"
version = "0.1.0"
description = "DebateItOut — multi-model AI debate orchestrator"
requires-python = ">=3.13"
dependencies = [
    "asyncpg>=0.31.0",
    "fastapi>=0.137.1",
    "httpx>=0.28.1",
    "python-dotenv>=1.2.2",
    "uvicorn[standard]>=0.49.0",
]
